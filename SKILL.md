---
name: work-notes
description: 工作日志助手 — 自动处理每日 git 提交记录录入、手动记录补充、周报汇总和月度 PMI 生成。当用户说「查看某仓库的提交日志并加入工作记录」「列一下本周工作成果」「生成周报」「生成 PMI」「记录：xxx」或类似意图时触发。此 skill 不绑定固定路径，支持任何位置的工作日志目录。
---

# Work Notes Skill

管理个人工作日志，支持：每日提交记录录入、手动记录补充、周工作汇总、月度 PMI 生成。

## 核心设计原则

- **不硬编码任何路径**。工作日志目录由用户指定，或从当前工作目录向上搜索识别。
- **支持多目录**。同一机器或同一用户可以维护多个独立的工作日志目录（如 `~/work/notes/`、`d:/project-x/notes/`）。
- **首次使用引导**。如果找不到工作日志目录，询问用户创建或指定。
- **记住目录**。同一对话内，后续操作复用已确定的目录。

## 工作日志格式规范

### 文件命名

- 周日志：`YYYY-MM-WN.md`（如 `2026-04-W4.md`），WN 为该月第几周
- PMI 月报：`PMI-YYYY-M.DD~M.DD.md`（如 `PMI-2026-3.15~4.14.md`）

### 目录结构

工作日志目录按文档类型分文件夹归类，避免所有文件平铺在一起：

```
<work-notes>/
├── weekly/          # 周工作日志 (YYYY-MM-WN.md)
├── weekly-reports/  # 周报 (Weekly-*.md)
├── pmi/             # 月度 PMI 报告 (PMI-*.md)
├── interns/         # 实习生任务独立文档 (Intern-Tasks.md)
├── projects/        # 项目相关散文档 (如 TSthPCGUnity Layouts.md)
└── todo.md          # 待办清单，留在根目录便于快速访问
```

规则：
- 周日志统一放 `weekly/`，周报放 `weekly-reports/`，PMI 月报放 `pmi/`
- 实习生任务单独成文档放 `interns/`，不混入周工作记录
- 项目级散文档（非周志/周报/PMI）放 `projects/`
- `todo.md` 保留在根目录，不进子文件夹

### 周日志内容结构

```markdown
# YYYY年M月第N周工作日志

周一 (MM-DD):
- 【项目名】具体工作内容

周二 (MM-DD):
```

### 项目标签规则

- 项目条目格式：`- 【项目名】具体内容`
- 非项目条目：`- 内容`（如 `- 周会：xxx`）
- 空天保留星期行，不删除

### PMI 月报格式（双语，按项目分节）

PMI 月报必须按项目分节，并在开头给出各项目的**工作占比**。

```markdown
# PMI 报告 - YYYY年M月D日 ~ M月D日

**工作占比：** 项目A ~XX%，项目B ~XX%

---

## 【项目A】

**中文概要：**
1、主题归纳条目（≤20字）
2、主题归纳条目（≤20字）

**English Summary:**
1、Theme bullet (concise)
2、Theme bullet (concise)

---

## 【项目B】

**中文概要：**
- 该项目的具体内容，不与其它项目关联着写

**English Summary:**
- Concrete description of this project only
```

规则：
- **工作占比**：统计区间内各 `weekly/` 文件中每条 `- 【项目名】` 的数量（每条记 1，含未打标签但属于该项目的条目），按比例换算成百分比，写在报告开头。
- **按项目分节**：每个项目用 `## 【项目名】` 单独成节，节内先中文后英文，两段之间空一行。
- **Copilot UE 插件**等条目多的项目：中文概要用数字编号 `1、2、3、`，每条为**主题归纳**（把同类工作合并），单条 **≤20 个汉字**，不逐条罗列每日记录。
- **其它项目**（如 RenderDoc Copilot）：写**该项目自身的具体内容与成果**，不要写成"使用 XX 项目的设计理念"等与其他项目关联的表述。
- 英文 Summary 与中文概要一一对应、同样编号。
- 文件名：`PMI-YYYY-M.DD~M.DD.md`，存于 `pmi/`。

## 目录识别逻辑（按优先级）

1. **用户指定路径**：用户明确说「`<路径>`的工作记录」「`<路径>`的日志」，直接用用户提供的路径。

2. **从当前工作目录向上搜索**：如果用户没有指定路径，从当前工作目录向上逐级查找，匹配条件：
   - 存在文件符合 `YYYY-MM-W*.md` 模式（周日志文件），或
   - 存在文件符合 `PMI-*.md` 模式（PMI 月报文件），或
   - 存在 `.worknotes` 标记文件（空文件，表示这是工作日志目录）
   - 搜索深度上限 5 层，超出则判定为找不到

3. **用户确认或创建**：找不到时，询问用户：
   - 「当前目录未识别为工作日志目录。指定已有目录路径，或输入新路径让我创建。」
   - 用户确认路径后，写入 `.worknotes` 标记文件

4. **多工作日志目录管理**：同一对话内如需切换目录，用户明确说「换到`<路径>`」时切换，并记住新路径。

## 核心工作流程

### 0. 写入前先核对当前日期（必做）

任何写入或编辑工作日志的操作前，先确认今天的真实日期（年/月/日/星期）：

- 用 `date +"%Y-%m-%d %A"` 或读取环境中的 Today's date 取当前日期。
- 用户说"今天/昨天/记录一下"等相对时间时，先换算成绝对日期再决定写入哪个 `YYYY-MM-WN.md` 和哪个星期小节。
- 跨周（周一）或跨月时尤其谨慎：确认是否需要新建 `YYYY-MM-WN.md` 文件，而不是续写上周。
- 用户未指定日期时默认按当前真实日期；存在歧义时先问一句再写。

**Why:** 上下文里的"今天"可能跟环境的真实日期不一致，凭印象写过把周一记录写到上周五的错误。

### 1. 每日提交记录录入

当用户说「查看`<路径>`里今天的提交日志，简化后翻译成中文加入今天的工作记录」或类似表述时：

**Step 1 — 确定工作日志目录**
按上述目录识别逻辑执行。用户提供路径时用用户路径，否则自动搜索。

**Step 2 — 确定日期**
- 今天：自动推断（查询 git log 时用 `--since="<today>T00:00:00" --until="<tomorrow>T00:00:00"`）
- 用户指定日期：用指定日期

**Step 3 — 确定周序号**
- 读取工作日志目录下所有 `YYYY-MM-W*.md` 文件
- 从文件名推断当前周的序号
- 如果该周文件不存在则创建新的周日志文件

**Step 4 — 查询 git 提交**
```bash
git -C "<repo-path>" log --since="<date>T00:00:00" --until="<next-date>T00:00:00" --oneline
```
- 用 `git log --oneline` 获取提交
- 注意：用户说"今天"但可能已过午夜，实际日期以 git commit 日期中的日期为准

**Step 5 — 追加到周日志**
读取现有 W 文件，将新条目按格式追加到对应星期下。
- 如果对应星期已有记录，检查是否重复（对比 commit hash 或内容），避免重复追加
- 如果该星期记录为空，直接替换空行

**Step 6 — 简化与翻译规则**
- 去除 commit hash，只保留语义
- 格式：`【项目名】简化后的中文描述`
- 项目名从 commit message 中提取 `feat/项目名:` 格式
- 如果不确定项目名，询问用户
- 如果 commit message 是纯英文，先翻译成中文再记录

### 2. 手动记录补充

当用户说「记录：`<内容>`」或类似表述时：
- 确认工作日志目录（同上）
- 确认日期（用户指定或今天）
- 追加到对应星期

### 3. 周工作汇总

当用户说「列一下本周目前的工作成果」「本周工作」「周报」或类似表述时：
- 找到当前 W 文件
- 读取全部内容
- 按星期分组，列出所有条目
- 简洁输出，突出重点

#### 周报总结格式（含条目标题，三方面各一句话）

当用户要求生成"周报总结"或按"需求/问题、产出/成果、预期/收益"三方面汇总时，必须先根据本周核心工作起一个条目标题，再按三方面各用一句话概括，要求高度精炼：

```markdown
**条目标题：** [基于本周核心工作起一个简洁标题]

**需求/问题：** [一句话概括本周要解决的核心问题与需求]

**产出/成果：** [一句话概括本周完成的关键产出]

**预期/收益：** [一句话概括这些工作带来的预期收益]
```

规则：
- 必须先起条目标题，再写三方面总结
- 每方面一句话，不逐条列举
- 用户要求更详细时再展开
- 先梳理本周日志条目，提炼核心主题起标题，再提炼成三句

### 4. PMI 月度报告生成

当用户说「生成 PMI」「月度汇总」或类似表述时：
- 确认工作日志目录
- 确定月份范围（通常上个月 15 日到本月 14 日，或询问用户）
- 收集区间内所有 `weekly/` 文件的条目，按项目归类
- **统计各项目条目数，算出工作占比**（每条记录计 1），写在报告开头
- 按项目分节生成：Copilot UE 插件等条目多的项目用编号 `1、2、3、` 主题归纳（单条 ≤20 字）；其它项目写自身具体内容、不关联其它项目
- 中英双语，中文概要在前、English Summary 在后
- 保存为 `PMI-YYYY-M.DD~M.DD.md` 格式，存于 `pmi/`

## 注意事项

- 所有文件路径操作使用 `/` 正斜杠，不用 `\`
- 追加文件前先 Read 确认内容，避免覆盖
- 重复检查：如果某条 commit 已经记录过，跳过
- 提交简化原则：去除技术细节，保留核心动作和结果
- 工作日志目录可以是独立的 git 仓库，也可以是普通文件夹

## 参考文件

- `references/work-notes-format.md` — 工作日志详细格式规则
- `references/pmi-format.md` — PMI 月报格式规范
